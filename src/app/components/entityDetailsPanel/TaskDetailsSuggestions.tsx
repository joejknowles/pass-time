import { Box, Typography } from "@mui/material";

export const TaskDetailsSuggestions = () => {
    return (
        <Box>
            <Typography variant="subtitle1">Suggestions / reminders</Typography>
            {/* Add content for suggestions and reminders here */}
        </Box>
    );
};
